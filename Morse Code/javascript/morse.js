/**
 * encode a message into morse code
 * @param {string[]} message the message to encode
 * @return string
 */
var encode = function (message) {
  // implementation
}

/**
 * decode a series of dots and dashes
 * @param {string[]} message the message to decode
 * @return string
 */
var decode = function (message) {
  // implementation
}


module.exports = { encode: encode, decode: decode };